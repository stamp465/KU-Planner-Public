export interface StdPlanRequest {
  campusCode: string;
  accessToken: string | null;
  cmasterId: string;
}

export interface StdPlanStructureRequest {
  accessToken: string | null;
  cmasterId: string;
}
